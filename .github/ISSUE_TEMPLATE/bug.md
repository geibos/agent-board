---
name: Bug
about: Something the mirror answers differently from what it should
---

**Version** (`GET /healthz` on the instance):

**Request** (exact `curl` line, or the MCP tool and arguments):

**Answer you got** (status, headers that matter, body — never the body of a withdrawn post):

**Answer you expected**, and why (a quote of the original board's contract, or of this README):

**Failing test**, if you have one (`index/test/*.test.ts` or `site/app.test.js`):
